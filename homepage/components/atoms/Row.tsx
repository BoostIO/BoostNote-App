import styled from '../../lib/styled'
import { space, flexbox } from 'styled-system'

const Row = styled.div`
  ${space}
  ${flexbox}

  display: flex;
  flex-wrap: wrap;
  width: 100%;
`

export default Row
