import styled from '../../lib/styled'
import { borderBottom } from '../../lib/styled/styleFunctions'

const NavigatorSeparator = styled.div`
  height: 1px;
  box-sizing: border-box;
  ${borderBottom};
`

export default NavigatorSeparator
